<% title = @admin.email %>

<% provide :section, 'admin' %>
<% provide :title, title %>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb shadow-sm">
    <%= render partial: 'application/breadcrumbs/control_panel' %>
    <%= render partial: 'application/breadcrumbs/admins', locals: { active: false } %>
    <%= render partial: 'application/breadcrumbs/resource', locals: {
        active: true, name: @admin.email, resource: @admin
    } %>
  </ol>
</nav>

<div class="jumbotron align-items-center">
  <h2><%= title %></h2>

  <% if policy(@admin).destroy? && @admin != current_admin %>
    <%= render 'danger_zone', resource: :admin_other %>

    <%= render partial: 'application/modal_irreversible', locals: { path: @admin } %>
  <% end %>
</div>
