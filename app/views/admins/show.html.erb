<% provide :title, @admin.email %>

<div class="jumbotron align-items-center">
  <h2><%= @admin.email %></h2>

  <% if policy(@admin).update? %>
    <%= link_to t(:edit), edit_admin_path(@admin) %> |
  <% end %>

  <%= link_to t(:back), admins_path %>
</div>
