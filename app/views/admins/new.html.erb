<% provide :title, t(:new, scope: :admin) %>

<div class="jumbotron align-items-center">
  <h2><%= t :new, scope: :admin %></h2>

  <%= render 'form', admin: @admin, password: false, submit_label: t(:create) %>

  <%= link_to t(:back), admins_path %>
</div>
