<% title = t :editing, scope: :resource, resource: @project.project_name %>

<% provide :section, 'project' %>
<% provide :title, title %>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb shadow-sm">
    <%= render 'application/breadcrumbs/control_panel' %>
    <%= render 'application/breadcrumbs/projects', active: false %>
    <%= render 'application/breadcrumbs/update', active: false, name: @project.project_name, resource: @project %>
  </ol>
</nav>

<div class="jumbotron align-items-center">
  <h2>
    <%= title %>
  </h2>

  <%= render 'form', project: @project, password: true, submit_label: t(:save) %>

  <div class="mt-4"></div>
  <%= render 'application/danger_zone', path: @project, resource: :project %>
</div>
