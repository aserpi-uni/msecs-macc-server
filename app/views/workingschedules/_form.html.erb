<%= form_with(model: workingschedule, local: true) do |form| %>
  <% if workingschedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workingschedule.errors.count, "error") %> prohibited this workingschedule from being saved:</h2>

      <ul>
        <% workingschedule.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hours %>
    <%= form.text_field :hours %>
  </div>

  <div class="field">
    <%= form.label :worker_id %>
    <%= form.text_field :worker_id %>
  </div>

  <div class="field">
    <%= form.label :activity_id %>
    <%= form.text_field :activity_id %>
  </div>

  <div class="field">
    <%= form.label :subactivity_id %>
    <%= form.text_field :subactivity_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
